{{- if eq .Values.mode "bundled" }}
Tenure is installed in bundled mode (self-contained MongoDB).

  kubectl port-forward svc/{{ include "tenure.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

⚠️  Bundled mode uses auto-generated secrets unless you provide them.
   If you reinstall or upgrade, capture the generated credentials from
   the generated Secret first, or set secrets.existingSecret.
{{- else }}
Tenure is installed in external mode.

  kubectl port-forward svc/{{ include "tenure.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

External MongoDB: {{ .Values.database.external.uri }}
User ID:          {{ .Values.identity.userId | default "team" }}
Secrets:          {{ .Values.secrets.existingSecret }}
{{- end }}